# ── System monitoring ────────────────────────────────────────────────────────
psutil>=5.9.0,<7.0          # RAM/CPU stats in /health endpoint

# ── Core Framework ──────────────────────────────────────────────────────────
fastapi>=0.111.0,<1.0
uvicorn[standard]>=0.29.0,<1.0
python-multipart>=0.0.9

# ── Database ─────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.30,<3.0
asyncpg>=0.29.0,<1.0
alembic>=1.13.1,<2.0

# ── Cache & Message Broker ───────────────────────────────────────────────────
# redis[asyncio] includes async support; replaces the deprecated aioredis package
redis[asyncio]>=5.0.4,<6.0
celery[redis]>=5.4.0,<6.0
flower>=2.0.1,<3.0
# Explicit override — flower pulls in old Pillow; Python 3.14 requires Pillow 11+
pillow>=11.0

# ── Auth ─────────────────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0,<4.0
bcrypt>=4.1.0,<5.0

# ── HTTP / Scraping ──────────────────────────────────────────────────────────
httpx>=0.27.0,<1.0
beautifulsoup4>=4.12.3,<5.0
lxml>=5.3.0               # 5.3+ ships Python 3.14 wheels
fake-useragent>=1.5.1,<2.0
tenacity>=8.3.0,<10.0
apify-client>=1.7.0,<2.0  # Apify actors (Blinkit, etc.)
playwright>=1.44.0,<2.0   # Headless browser — bypass Cloudflare on all platforms

# ── Validation & Settings ────────────────────────────────────────────────────
pydantic>=2.7.1,<3.0
pydantic-settings>=2.2.1,<3.0
email-validator>=2.1.1,<3.0

# ── WebSocket & Async ────────────────────────────────────────────────────────
websockets>=12.0,<15.0
aiofiles>=23.2.1,<25.0

# ── Monitoring & Logging ─────────────────────────────────────────────────────
prometheus-fastapi-instrumentator>=7.0.0,<8.0
structlog>=24.2.0,<26.0
sentry-sdk[fastapi]>=2.3.1,<3.0

# ── Utilities ────────────────────────────────────────────────────────────────
python-slugify>=8.0.4,<9.0
boto3==1.34.114
pytz==2024.1
arrow==1.3.0

# ── Push notifications (Firebase Cloud Messaging — FCM HTTP v1) ──────────────
firebase-admin>=6.5.0,<7.0

# ── Social automation (optional — X/Twitter posting) ────────────────────────
tweepy>=4.14.0,<5.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0
httpx==0.27.0
factory-boy==3.3.0
