# API calls are proxied by Next.js — browser only needs port 3000 open
NEXT_PUBLIC_API_URL=
# WebSocket connects directly (browser → server); update host as needed
NEXT_PUBLIC_WS_URL=ws://test.pricebasket.in:8000
