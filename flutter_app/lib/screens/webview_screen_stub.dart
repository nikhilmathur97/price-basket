// This stub file is the fallback for the conditional import in webview_screen.dart.
// In practice, either dart.library.io (mobile/desktop) or dart.library.html (web)
// is always available, so this file is never actually compiled.
// It re-exports the web stub to avoid any webview_flutter dependency.
export 'webview_screen_web.dart';
